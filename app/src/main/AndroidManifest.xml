<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permisos del flujo de reporte (pedidos en tiempo de ejecución en C06) -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />

    <application
        android:name=".MinkIaApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MinkIA"
        tools:targetApi="31">

        <activity
            android:name=".ui.splash.SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.MinkIA.Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ui.onboarding.OnboardingActivity"
            android:exported="false" />

        <activity
            android:name=".ui.login.LoginActivity"
            android:exported="false" />

        <activity
            android:name=".ui.registro.RegistroActivity"
            android:exported="false" />

        <activity
            android:name=".ui.recuperar.RecuperarActivity"
            android:exported="false" />

        <activity
            android:name=".ui.permisos.PermisosActivity"
            android:exported="false" />

        <activity
            android:name=".ui.captura.CapturaActivity"
            android:exported="false"
            android:screenOrientation="portrait" />

        <activity
            android:name=".ui.analisis.AnalisisActivity"
            android:exported="false" />

        <activity
            android:name=".ui.formulario.FormularioActivity"
            android:exported="false" />

        <activity
            android:name=".ui.confirmacion.ConfirmacionActivity"
            android:exported="false" />

        <activity
            android:name=".ui.detalle.DetalleActivity"
            android:exported="false" />

        <activity
            android:name=".ui.notificaciones.NotificacionesActivity"
            android:exported="false" />

        <activity
            android:name=".ui.configuracion.ConfiguracionActivity"
            android:exported="false" />

        <activity
            android:name=".ui.MainActivity"
            android:exported="false" />

        <activity
            android:name=".ui.admin.AdminMainActivity"
            android:exported="false" />

        <activity
            android:name=".ui.admin.ValidacionActivity"
            android:exported="false" />
    </application>

</manifest>
