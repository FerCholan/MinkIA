{
  "name": "minkia-reglas-firestore",
  "version": "1.0.0",
  "private": true,
  "description": "Pruebas de las reglas de seguridad de Cloud Firestore contra el emulador de Firebase.",
  "type": "module",
  "scripts": {
    "test": "firebase emulators:exec --config ../../firebase.json --only firestore --project minkia-reglas \"node --test\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^4.0.1",
    "firebase": "^11.0.0",
    "firebase-tools": "^14.0.0"
  }
}
